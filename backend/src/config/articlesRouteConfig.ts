const maxResponseCount = 10000;

const filtersDefinition = {
    keywordStringType: [
        'tags',
        'name',
        'content',
    ],
    keywordNumberType: [
    ]
}

export {
    maxResponseCount,
    filtersDefinition
}